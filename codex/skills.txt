# Codex skills to auto-install via setup.sh
# One skill name per line (from openai/skills curated list)
# Lines starting with # are ignored
# Note: context7 is installed separately via ~/.agents/skills symlink
playwright
pdf
jupyter-notebook
